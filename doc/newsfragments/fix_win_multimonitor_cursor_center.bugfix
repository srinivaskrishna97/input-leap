Fix Windows bug where the cursor jumped to the center of the wrong monitor when entering a screen that has multiple monitors. Absolute mouse positioning now normalizes over the whole virtual desktop (MOUSEEVENTF_VIRTUALDESK and the virtual-screen origin) instead of only the primary monitor, so positions on secondary monitors and monitors with negative coordinates are placed correctly.
